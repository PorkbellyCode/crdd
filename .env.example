# Turso (운영). 비우면 로컬 SQLite 파일(file:./local.db)로 돈다
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# 로그인 (Auth.js v5)
# AUTH_SECRET은 `bunx auth secret` 또는 `openssl rand -base64 33`으로 만든다
AUTH_SECRET=
# GitHub OAuth App — Redirect URI: <주소>/api/auth/callback/github
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
